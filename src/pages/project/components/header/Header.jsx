import React from "react";
import "./Header.scss";
import "./Header.responsive.scss";

export default ({project}) => {
    return (
        <header id="project-header">
            Header works!
        </header>
    )
}